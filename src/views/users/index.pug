extends ../layouts/page.pug

block content
  h1 Пользователи
  form.input-group.my-4(role = 'form' method = 'GET' action = '/users')
    input.form-control.rounded(type = 'search' name = 'term' value = form.term)
    input.btn.btn-outline-primary(type = 'submit' value = 'search')
  if users.length === 0
    p Пользователи не найдены
  else
    table.table.table-striped
      tr
        th Идентификатор пользователя
        th Имя пользователя
        th Емейл пользователя
      each user, index in users
        tr
          td
            a(href='/users/' + user.id) #{user.id}
          td= user.name
          td= user.email
  a.btn.btn-primary(href='/users/new') Новый пользователь